# Privacy architecture

The public repository contains only schemas and fictional examples. Real user data is stored under the Git-ignored `workspace/` directory.

The database can contain sensitive application history. Back it up only to storage you trust.

When using an API-based model, the minimum necessary context is sent for each task. Avoid including unrelated personal information in the candidate source folder.
